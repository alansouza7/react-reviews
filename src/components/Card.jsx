/* import Cards from "./Cards"


const Card = (props) => {

    const {data} = props


  return (
    <section>
        {data.map(person=>{
            return <Cards key={person.id} {...person} />
        })}
    </section>
  )
}
export default Card */